import { Calendar } from "./components/Calendar";

export const CalendarPage = () => {
    return (
        <section>
            <h2>CALENDAR</h2>

            <Calendar />
        </section>
    );
};
